# Frontend

Thư mục này chứa frontend tách riêng (Vite) cho dự án.

## Chạy local

```bash
cd frontend
npm install
npm run dev
```

Frontend hiển thị trang điều hướng và liên kết nhanh đến backend Streamlit đang chạy ở `http://localhost:8501`.